specVersion: "v1beta"
serviceId: "spinapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "spinapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
